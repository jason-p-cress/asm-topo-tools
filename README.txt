These scripts expect to be installed in $ASM_HOME/cleantopology

The following directories need to be created:

$ASM_HOME/cleantopology/backups		--directory to backup existing topologies
$ASM_HOME/cleantopology/workdir		--directory to save tool and UI customizations
